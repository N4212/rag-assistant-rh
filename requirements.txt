# Assistant RAG RH — dépendances directes
# Python 3.14 requis
# Installation : pip install -r requirements.txt

# --- Pipeline RAG ---
chromadb==1.5.9
sentence-transformers==6.0.1
torch==2.14.0
google-genai==2.22.0

# --- Configuration ---
python-dotenv==1.2.2
requests==2.33.1

# --- MLOps ---
mlflow==3.16.0
prefect==3.8.5

# --- Composants externes (non installés par pip) ---
# n8n 2.38.6 — conteneur Docker :
#   docker run -d --name n8n -p 5678:5678 -v n8n_data:/home/node/.n8n docker.n8n.io/n8nio/n8n